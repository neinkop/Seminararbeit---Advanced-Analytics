\chapter{Hauptteil}

\section{Technische Architektur}

\begin{figure}[htb]
    \centering
    \includegraphics[width=\textwidth]{Abbildungen/Containerarchitektur.png}
    \caption[Containerarchitektur]{Containerarchitektur\footnotemark}
    \label{fig:containerarchitektur}
\end{figure}
\footnotetext{Eigene Darstellung.}

Die realisierte Systemarchitektur setzt sich aus sechs funktional getrennten Docker-Containern zusammen, die über ein gemeinsames Bridge-Netzwerk kommunizieren (vgl. Abbildung~\ref{fig:containerarchitektur}). Das Fundament bilden drei identische Instanzen des Aufbereitungsdienstes, die durch containerindividuelle Volume-Mounts jeweils einen spezifischen Datenzeitraum verarbeiten: \texttt{aa\_aufbereitung\_1} verarbeitet die Daten des Jahres 2024, \texttt{aa\_aufbereitung\_2} die des Jahres 2025 und \texttt{aa\_aufbereitung\_3} die des Jahres 2026. Die mehrfache Instanziierung desselben Container-Images demonstriert das Kernprinzip horizontaler Skalierbarkeit, bei der eine einmal definierte Laufzeitumgebung ohne Anpassung des Anwendungscodes für unterschiedliche Eingabedaten repliziert werden kann (vgl. Merkel 2014, S.~4~f.). Jede Instanz exponiert eine REST-konforme HTTP-Schnittstelle und stellt aufbereitete Datensätze sowie Spalteninformationen für nachgelagerte Dienste bereit. Um eine kontrollierte Startreihenfolge zu gewährleisten, definiert \texttt{docker-compose.yaml} Gesundheitsprüfungen auf Basis von HTTP-Anfragen sowie \texttt{depends\_on}-Bedingungen, die sicherstellen, dass kein nachgelagerter Dienst startet, bevor alle Aufbereitungsinstanzen betriebsbereit sind (vgl. Stoneman 2020, S.~14~ff.).

Der Forecast-Container bezieht seine Trainingsdaten ausschließlich von \texttt{aa\_aufbereitung\_1} (2024) und \texttt{aa\_aufbereitung\_2} (2025). Die Daten des Jahres 2026 aus \texttt{aa\_aufbereitung\_3} bleiben dem Forecast-Dienst bewusst vorenthalten, sodass die für 2026 generierten Prognosen mit den tatsächlich beobachteten Werten verglichen werden können, ohne dass das Modell diese Daten während des Trainings gesehen hat. Diese strikte Trennung von Trainings- und Validierungsdaten ist eine grundlegende Anforderung an die Generalisierbarkeit statistischer Modelle (vgl. Fahrmeir/Tutz 2001, S.~86~ff.). Der API-Container aggregiert dagegen die Daten aller drei Aufbereitungsinstanzen und dient als vollständiger Datenzugangspunkt für die Visualisierungsschicht. Den Abschluss des Datenflusses bildet der Visualisierungs-Container, der eine interaktive Webanwendung bereitstellt und sowohl historische als auch prognostizierte Kennzahlen grafisch aufbereitet. Die konsequente Diensttrennung entspricht dem Microservice-Paradigma, das funktionale Einheiten in lose gekoppelte, unabhängig deploybare Dienste zerlegt (vgl. Newman 2015, S.~1~ff.).

\section{Aufbereitung der Daten}

Die Datenaufbereitung gliedert sich in drei aufeinanderfolgende Phasen: Datenimport, Bereinigung und Merkmalsgenerierung. Der Import unterstützt sowohl das CSV- als auch das spaltenorientierte Apache-Parquet-Format, wobei Parquet-Dateien blockweise über PyArrow eingelesen werden, um den Hauptspeicherverbrauch zu kontrollieren (vgl. Vohra 2016, S.~11~f.). CSV-Dateien werden mittels \texttt{pandas} in Chunks von 100.000~Zeilen verarbeitet; im Testbetrieb wird über zufälliges Zeilen-Sampling der Datensatz auf einen repräsentativen Bruchteil reduziert (vgl. McKinney 2017, S.~97~ff.).

Die Bereinigungsroutine entfernt systematisch unplausible Datensätze: Fahrten mit einem Fahrpreis kleiner oder gleich null, Fahrtstrecken außerhalb des Intervalls $(0, 100]$~Meilen sowie Passagierzahlen kleiner eins oder größer sechs werden ausgeschlossen. Extremwerte des Fahrpreises oberhalb des 99,5.~Perzentils werden gekappt, um den Einfluss vereinzelter Datenerfassungsfehler auf nachgelagerte Modelle zu begrenzen.

Die Merkmalsgenerierung überführt die Rohdaten in ein für statistische Modellierung geeignetes Format. Aus dem Abholzeitpunkt werden Wochentag, Jahreszeit sowie eine Reihe binärer Indikatoren abgeleitet: Wochenende (\texttt{is\_weekend}), gesetzlicher Feiertag nach US-amerikanischem Kalender (\texttt{is\_holiday}), Schulferien (\texttt{is\_school\_holiday}), Vortag und Folgetag eines Feiertags (\texttt{before\_holiday}, \texttt{after\_holiday}), besonderes Ereignis (\texttt{is\_event}) sowie Hauptverkehrszeit zwischen 7--9~Uhr und 16--18~Uhr (\texttt{is\_rush\_hour}). Zur Abbildung der inhärenten Zyklizität zeitlicher Dimensionen werden Wochentag und Monat mittels trigonometrischer Transformation kodiert:

\begin{equation}
    x_{\sin} = \sin\!\left(\frac{2\pi \cdot t}{T}\right), \quad x_{\cos} = \cos\!\left(\frac{2\pi \cdot t}{T}\right)
\end{equation}

Dabei bezeichnet $t$ den diskreten Zeitindex (Wochentag $0$--$6$ bzw. Monat $1$--$12$) und $T$ die Periodenlänge. Diese Kodierung stellt sicher, dass die Distanz zwischen Montag und Sonntag im Merkmalsraum der intuitiven zyklischen Nähe entspricht, eine Eigenschaft, die lineare Merkmale nicht abbilden können (vgl. Fahrmeir/Tutz 2001, S.~10~f.).

Für Fahrten mit Barzahlung weist der Datensatz strukturell fehlende Trinkgeldwerte auf, da diese Zahlungsart keine elektronische Erfassung ermöglicht. Zur Imputation wird das in Abschnitt~\ref{sec:mice} beschriebene MICE-Verfahren eingesetzt. Als erklärende Größen dienen dabei Fahrpreis, Fahrtdistanz und Gesamtbetrag: Trinkgelder fallen typischerweise proportional zum Fahrpreis aus, da Fahrgäste einen prozentualen Anteil des gezahlten Betrags als Trinkgeld geben. Die Fahrtdistanz ist eng mit dem Fahrpreis korreliert und liefert zusätzliche Information über den Charakter der Fahrt. Der Gesamtbetrag ergänzt diese Größen, da er neben dem Fahrpreis auch anfallende Zuschläge und Steuern enthält und damit den vollständigen Zahlungskontext abbildet. Da alle drei Variablen auch für Barzahlungsfahrten vollständig vorliegen, eignen sie sich als zuverlässige Grundlage für die Schätzung der fehlenden Trinkgeldwerte. Negative Imputationswerte werden anschließend auf null begrenzt, da Trinkgelder definitionsgemäß nicht-negativ sind (vgl. Pedregosa et~al. 2011, S.~2826).

\section{Modellierung und Prognose}

Der Forecast-Dienst realisiert eine anfrage-gesteuerte statistische Modellierung: Für jede eingehende HTTP-Anfrage werden Aggregationsmethode, Gruppierungsdimensionen und Zielvariable aus den URL-Parametern extrahiert, Modelle auf den aggregierten Trainingsdaten der Jahre 2024 und 2025 angepasst und Vorhersagen für den Zielzeitraum 2026 generiert.

\subsection{Formelkonstruktion und Merkmalsauswahl}

Die Modellformel wird dynamisch aus den in der Anfrage übergebenen Gruppierungsdimensionen und dem jeweiligen Zeitgranular zusammengesetzt. Das Grundprinzip besteht darin, die nutzerdefinierten Dimensionen um kontextabhängige erklärende Variablen zu ergänzen, die das Nachfrageverhalten strukturell beschreiben. Für tagesgenaue Anfragen werden stets die zyklischen Wochentag-Kodierungen sowie alle binären Tagesindikatoren ergänzt; für monatliche Anfragen kommen die trigonometrischen Monatskodierungen und die Jahreszeit hinzu. Sind stündliche Dimensionen wie \texttt{pickup\_hour} oder \texttt{dropoff\_hour} Teil der Gruppierung, wird zusätzlich der Hauptverkehrszeit-Indikator aufgenommen. Tabelle~\ref{tab:features} fasst die Merkmale nach Kontext zusammen.

\begin{table}[htb]
\centering
\caption[Merkmale nach Zeitgranular]{Automatisch ergänzte Merkmale nach Zeitgranular\footnotemark}
\label{tab:features}
\begin{tabular}{lp{9cm}}
\toprule
\textbf{Kontext}       & \textbf{Ergänzte Merkmale} \\
\midrule
Tagesgenaue Anfrage    & \texttt{dow\_sin}, \texttt{dow\_cos}, \texttt{C(is\_weekend)},
                         \texttt{C(is\_holiday)}, \texttt{C(is\_school\_holiday)},
                         \texttt{C(before\_holiday)}, \texttt{C(after\_holiday)},
                         \texttt{C(is\_event)} \\[4pt]
Monatliche Anfrage     & \texttt{month\_sin}, \texttt{month\_cos}, \texttt{C(season)} \\[4pt]
Stündliche Dimension   & \texttt{C(is\_rush\_hour)} \\
\bottomrule
\end{tabular}
\end{table}
\footnotetext{Eigene Darstellung.}

Kategoriale Merkmale werden durch Dummy-Kodierung $C(\cdot)$ in binäre Kontrastvariablen überführt; zyklische Merkmale verbleiben als numerische Variablen in der Formel. Für ein tagesgenaues Beispiel mit der Dimension \texttt{payment\_type} und der Zielvariablen \texttt{fare\_amount} ergibt sich die folgende Modellformel:

\begin{equation}
\begin{split}
    \texttt{fare\_amount} \sim\; & C(\texttt{payment\_type}) + \texttt{dow\_sin} + \texttt{dow\_cos} \\
    & +\; C(\texttt{is\_weekend}) + C(\texttt{is\_holiday}) + C(\texttt{is\_school\_holiday}) \\
    & +\; C(\texttt{before\_holiday}) + C(\texttt{after\_holiday}) + C(\texttt{is\_event})
\end{split}
\end{equation}

Für die Prognose wird ein vollständiges kartesisches Produkt aller zulässigen Merkmalsausprägungen aufgespannt: Alle Kombinationen der angeforderten Dimensionen werden mit den für das konkrete Prognosedatum deterministisch berechneten Tages- und Saisonmerkmalen gekreuzt. Der Wochentag, die Feiertagsindikatoren und die Sinus-/Kosinus-Werte werden dabei aus dem angefragten Datum abgeleitet, sodass für jeden Prognosepunkt ein vollständiger Merkmalsvektor vorliegt.

\subsection{GLM-Familien und Modellauswahl}

Je nach Aggregationsmethode der Zielvariablen werden unterschiedliche Verteilungsfamilien des GLM eingesetzt, da die klassische lineare Regression Annahmen trifft, namentlich Normalverteilung und konstante Streuung, die für Zähldaten oder strikt positive Mengengrößen verletzt sind (vgl. McCullagh/Nelder 1989, S.~1~ff.). Tabelle~\ref{tab:glm_familien} gibt einen Überblick der verwendeten Familien.

\begin{table}[htb]
\centering
\caption[Verwendete GLM-Familien]{Verwendete GLM-Familien und ihre Einsatzgebiete\footnotemark}
\label{tab:glm_familien}
\small
\begin{tabular}{p{3.2cm}p{1.5cm}p{2cm}p{5.5cm}}
\toprule
\textbf{Familie} & \textbf{Methode} & \textbf{Verknüpfung} & \textbf{Anwendungsfall} \\
\midrule
Poisson           & count & Log       & Zähldaten, Varianz gleich Erwartungswert \\
Negativbinomial   & count & Log       & Zähldaten mit Überdispersion \\
Gauß              & mean  & Identität & Stetige Zielgrößen, Referenzmodell \\
Gamma             & mean  & Log       & Positive, rechtsschiefe Größen \\
Invers-Gauß       & mean  & Log       & Positive, stark rechtsschiefe Größen \\
OLS               & beide & keine     & Lineares Referenzmodell \\
\bottomrule
\end{tabular}
\end{table}
\footnotetext{Eigene Darstellung nach McCullagh/Nelder 1989 und Cameron/Trivedi 2013.}

Bei Zählaggregationen kommt zunächst ein \textbf{Poissonmodell} zum Einsatz. Es modelliert nicht-negative ganzzahlige Zähldaten unter der Annahme, dass Erwartungswert und Varianz identisch sind. Die logarithmische Verknüpfungsfunktion stellt dabei sicher, dass vorhergesagte Zählwerte stets nicht-negativ sind (vgl. Cameron/Trivedi 2013, S.~75~f.). In Taxifahrtdaten tritt jedoch häufig Überdispersion auf: Wetter, Großveranstaltungen oder Feiertage führen dazu, dass Fahrten nicht gleichmäßig über die Zeit verteilt auftreten, sondern in Schüben. Die tatsächliche Streuung übersteigt damit den vom Poissonmodell erwarteten Wert erheblich, was zu zu engen Konfidenzintervallen und damit irreführenden Signifikanztests führt. Das \textbf{Negativbinomialmodell} begegnet diesem Problem, indem es einen zusätzlichen Dispersionsparameter $\alpha$ einführt, der die Varianz auf $\mu + \alpha\mu^2$ ausweitet und so die überschüssige Streuung explizit modelliert (vgl. Cameron/Trivedi 2013, S.~75~f.; Nelder/Wedderburn 1972, S.~370~ff.).

Bei stetigen Zielgrößen steht zunächst das \textbf{Gaußmodell} zur Verfügung, das einer linearen Regression mit Normalverteilungsannahme entspricht und als Referenzmodell dient. Für strikt positive Zielgrößen wie Fahrpreise oder Trinkgelder werden zusätzlich ein \textbf{Gammamodell} sowie ein \textbf{Invers-Gauß-Modell} angepasst. Das Gammamodell eignet sich besonders für Größen, bei denen die Streuung proportional zum Erwartungswert wächst, das heißt, hohe Fahrpreise streuen stärker als niedrige, was bei langen Fahrten typischerweise beobachtet wird (vgl. McCullagh/Nelder 1989, S.~292~ff.). Das Invers-Gauß-Modell ist für Situationen geeignet, in denen extrem hohe Einzelwerte noch ausgeprägter vom Durchschnitt abweichen, als es das Gammamodell annehmen würde. Vereinfacht gesprochen modelliert das Gammamodell eine moderate Zunahme der Streuung mit dem Erwartungswert, während das Invers-Gauß-Modell eine noch stärkere Zunahme annimmt und damit seltene, aber sehr hohe Werte, wie teure Flughafenfahrten, besser abbildet (vgl. McCullagh/Nelder 1989, S.~490~ff.).

Für stetige Zielgrößen wird vor der Modellanpassung eine logarithmische Transformation der Zielvariablen mittels $\log(1 + y)$ vorgenommen. Der Hintergrund ist, dass finanzielle Größen wie Fahrpreise typischerweise rechtsschief verteilt sind: Günstige Kurzfahrten kommen sehr häufig vor, teure Langstreckenfahrten hingegen selten, aber mit großer Streuung. Lineare Modelle gehen implizit von symmetrisch verteilten Abweichungen aus; bei rechtsschiefen Daten führt dies dazu, dass hohe Werte systematisch unter- oder überschätzt werden. Die Transformation $\log(1 + y)$ streckt den unteren Wertebereich und staucht hohe Werte, sodass die transformierte Zielvariable deutlich symmetrischer verteilt ist und die Modellannahmen besser erfüllt sind. Der additive Term~1 verhindert dabei, dass der Logarithmus bei Nullwerten undefiniert wird. Die Rücktransformation in die ursprüngliche Skala erfolgt über $\exp(\hat{y}) - 1$ (vgl. Fahrmeir/Tutz 2001, S.~18~f.). Ergänzend wird stets ein OLS-Modell als Referenzmodell geschätzt; alle Modellparameter werden über Maximum-Likelihood-Schätzung bestimmt (vgl. Seabold/Perktold 2010, S.~93).

\subsection{Modellbewertung über das AIC}

Zur modellklassenübergreifenden Bewertung der angepassten Modelle wird das Akaike-Informationskriterium (AIC) herangezogen, das durch

\begin{equation}
    \text{AIC} = -2 \cdot \ln(\hat{L}) + 2k
\end{equation}

definiert ist, wobei $\hat{L}$ den maximierten Wert der Likelihoodfunktion und $k$ die Anzahl der geschätzten Modellparameter bezeichnet (vgl. Fahrmeir/Tutz 2001, S.~86~ff.). Der erste Term misst die Anpassungsgüte an die beobachteten Daten: Je besser ein Modell die Daten erklärt, desto höher ist der Likelihoodwert und desto niedriger fällt dieser Term aus. Der zweite Term wirkt als Strafterm für Modellkomplexität, da jeder zusätzliche Parameter das AIC um zwei Einheiten erhöht. Das AIC sucht damit das Modell, das die Daten möglichst gut beschreibt, ohne unnötig viele Parameter zu verwenden. Konkret bedeutet ein niedrigerer AIC-Wert, dass ein Modell entweder bei gleicher Parameterzahl eine bessere Datenanpassung liefert oder bei vergleichbarer Anpassungsgüte mit weniger Parametern auskommt. Ein wesentlicher Vorteil gegenüber klassischen Gütekriterien wie $R^2$ ist die Anwendbarkeit über verschiedene Modellklassen und Verteilungsfamilien hinweg, was einen direkten Vergleich von Poissonmodell, Gammamodell und OLS ermöglicht (vgl. Fahrmeir/Tutz 2001, S.~86~ff.). Die Modellzusammenfassung, bestehend aus AIC, Koeffizientenschätzungen und zugehörigen $p$-Werten, wird über die API an die Visualisierungsschicht übermittelt und dort nach AIC aufsteigend sortiert dargestellt.

\section{Visualisierung}

Die Visualisierungsschicht ist als interaktive Webanwendung in Streamlit realisiert und bindet historischen API-Dienst sowie Forecast-Dienst als konfigurierbare Datenquellen ein (vgl. Tamminga 2022, S.~1~f.). Verfügbare Zeiträume werden dynamisch aus dem \texttt{/data\_summary}-Endpunkt der verbundenen APIs bezogen. Für die Ergebnisdarstellung stehen fünf Diagrammtypen zur Verfügung: Liniendiagramme für zeitliche Verläufe, Balkendiagramme für Vergleiche, Heatmaps für zweidimensionale Kreuztabellen sowie Streu- und dreidimensionale Streudiagramme. Sind beide Datenquellen aktiv, werden Prognose- und Ist-Daten für identische Zeitperioden kombiniert, sodass ein unmittelbarer visueller Abgleich der Modellgüte möglich ist.

\section{Mögliche Analysen}

Das realisierte System ermöglicht eine Vielzahl deskriptiver und prädiktiver Analysen. Deskriptiv lassen sich Nachfrageschwankungen über Tagesstunden, Wochentage und Monate verfolgen und mit Feiertagen oder Hauptverkehrszeiten in Beziehung setzen. Die Gruppierung nach Zahlungsart erlaubt Rückschlüsse auf Kundenpräferenzen; eine Kreuzauswertung von Anbieter und Tarifzone gibt Hinweise auf räumliche Nachfragekonzentrationen. Durch Gegenüberstellung der imputierten Trinkgelddaten mit Fahrpreisen lassen sich Einflussfaktoren auf das Trinkgeldverhalten explorieren.

Prädiktiv kann das System Zielgrößen wie Fahrtenanzahl, durchschnittlicher Fahrpreis oder Trinkgeldhöhe für beliebige Kombinationen aus Tageszeit, Wochentag und Saisonmerkmalen im Prognosezeitraum 2026 schätzen. Da die Modelle ausschließlich auf Daten der Jahre 2024 und 2025 trainiert wurden, können die generierten Prognosen unmittelbar mit den tatsächlich über \texttt{aa\_aufbereitung\_3} verfügbaren Beobachtungsdaten des Jahres 2026 verglichen werden. Der modellübergreifende Vergleich über das AIC ermöglicht dabei eine datengetriebene Auswahl des geeignetsten Modells je Anwendungsfall.